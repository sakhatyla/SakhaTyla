@using SakhaTyla.Core.Requests.Widgets
@inject MediatR.IMediator Mediator
@{
    var footerWidget = await Mediator.Send(new GetWidget() { Code = "footer" });
}

<partial name="_WidgetPartial" model="footerWidget" />
